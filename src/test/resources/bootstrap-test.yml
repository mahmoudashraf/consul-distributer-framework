spring:  
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: localhostrrr
        
logging:
  level:
    com.consul.leader.elections.*: TRACE
  pattern:
    console: d{} - %msg%n
    
server.port: 0