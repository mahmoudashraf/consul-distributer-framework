spring:  
  application:
    name: customer-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: localhostrrr

logging:
  level:
    pl.piomin.services.customer.*: TRACE