spring:  
  application:
    name: customer-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: localhostrrr

logging:
  level:
    com.consul.leader.elections.*: TRACE